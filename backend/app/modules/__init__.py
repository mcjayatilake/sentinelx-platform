"""Domain capability modules (scanning engines, testing, reporting, ...).

Each subpackage owns one product capability and will gain its own models,
schemas, services, API routers, and Celery tasks as it is implemented.
"""
